import React from "react";
import styles from "./progressBar.module.css";

// UI Component for the Bar for the animation between questions!

const ProgressBar = () => {
  return <div className={styles.progressBar}></div>;
};

export default ProgressBar;
